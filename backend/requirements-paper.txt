# The parser worker consumes only the `paper` queue.  Keep its dependency set
# separate from media/ASR/TTS packages: those pull a second ONNX/Transformers
# stack that is both unnecessary for extraction and can conflict with Docling.
celery[redis]==5.6.3
cryptography==49.0.0
httpx==0.28.1
pydantic-settings==2.14.2
python-frontmatter==1.3.0
python-slugify==8.0.4
redis==6.4.0
scipy==1.18.0  # required at runtime by Docling's Tesseract CLI adapter
sqlmodel==0.0.39

# Keep this exact pin aligned with app.paper.DOCLING_VERSION and rebuild the
# paper-worker image to upgrade.  Slim's explicit extras provide the CLI, PDF
# backend, and local models without Docling's broad `standard` dependency set.
docling-slim[cli,format-pdf,models-local]==2.113.0
opencv-python-headless==5.0.0.93
